#if !defined(TRADINGLIB_HPP)
#define TRADINGLIB_HPP

namespace trading {

    enum Price_mark 
    {
        close,
        open,
        high,
        low
    };

};

#endif //#if !defined(TRADINGLIB_HPP)